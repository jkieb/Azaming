{
  "name": "azaming",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Azan web app for Vienna with prayer times from IGGOe (derislam.at)",
  "scripts": {
    "discover": "node scripts/discover.mjs",
    "fetch": "node scripts/fetch-times.mjs",
    "serve": "npx --yes serve public",
    "smoke": "node scripts/smoke-test.mjs",
    "test": "node --test scripts/*.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
